Une fois qu'on a créé sa carte mentale, on peut la partager ou l'enregistrer de différentes façons.

### Option recommandée : héberger le fichier Markdown

Il est recommandé d'enregistrer le texte de sa carte mentale quelque part pour pouvoir modifier plus tard sa carte mentale, de préférence sur [CodiMD](https://codimd.apps.education.fr) ou une [forge](https://docs.forge.apps.education.fr/). Dans tous les cas, il faut que le fichier Markdown soit accessible publiquement via une URL.

Votre carte mentale sera alors à l'adresse suivante : `https://mymarkmap.forge.apps.education.fr/#URL` (en remplaçant `URL` par l'adresse où est hébergé votre fichier Markdown).

L'intérêt est que vous pourrez modifier votre carte mentale à tout moment en modifiant le fichier Markdown, sans changer l'URL de la carte mentale.

### Autres options de partage

Vous pouvez aussi :
- enregistrer la carte mentale au format _svg_ (clic sur 💾) ou _html_ (clic sur 🌐)
- créer un lien de partage rapide de votre carte mentale (clic sur 🔗)


### Cacher le menu

On peut cacher le menu en ajoutant le paramètre `?m=0` dans l'URL : `https://mymarkmap.forge.apps.education.fr/#URL?m=0`

On peut aussi configurer cette option par défaut dans son fichier Markdown, dans l'en-tête YAML avec la ligne suivante : `showMenu: false`

On peut enfin utiliser le raccourci clavier `m` pour masquer ou réafficher la barre de menu.